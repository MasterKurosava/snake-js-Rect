const width=600,
      height=600,
      ceil=30,
      rows=width/ceil,
      colors={
        snakeHead: "#00ffff",
        snakeBody: "#1fb9dd",
        wall: "#425870",
        apples: "#d86464"
      }

export {width, height, rows, colors};